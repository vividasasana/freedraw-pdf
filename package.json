{
  "name": "freedraw-pdf-workspace",
  "version": "0.13.2",
  "private": true,
  "description": "Versioned Freedraw PDF source; v2 is the active plugin.",
  "license": "MIT",
  "scripts": {
    "dev": "npm --prefix v2 run dev",
    "build": "npm --prefix v2 run build",
    "check": "node scripts/check-repository.js && npm --prefix v2 run check",
    "check:privacy": "node scripts/check-repository.js",
    "package": "npm run check && npm --prefix v2 run package",
    "vault:test": "npm --prefix v2 run vault:test"
  },
  "devDependencies": {
    "@types/node": "^18.19.130",
    "esbuild": "^0.28.1",
    "obsidian": "^1.12.3",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "perfect-freehand": "^1.2.3"
  }
}
